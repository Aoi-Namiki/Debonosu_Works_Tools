游戏脚本 `.scb` 为 lua5.1 的字节码，使用现成反编译器反编译，工具来源已经标注在主 readme；`.esc` 是纯文本，存的是环境变量不需要动。

注意运行需要 JRE ，最好是 8+，编译回去的话直接复用游戏下的 lua5.1.dll ，需要注意是 32 位的，Python 也必须使用 32 位。

需要注意的是，仅是做汉化的话，不建议将其反编译后汉化再编译回去，风险更高；建议只提取 luac 内的常量，建议单独 import，这样更稳定。